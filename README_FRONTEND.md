Next.js + Supabase starter

Commands:

- Install dependencies: `npm install`
- Dev: `npm run dev`
- Build: `npm run build`

Notes:
- Environment variables: `SUPABASE_URL` and `SUPABASE_KEY` are read from process env.
- Vercel already has these variables configured for this project.
